load('')
for (k=1:18)

    figure(k);

%     surf(med.X/1000,med.Y/1000,abs(med.Bx{k}));
    surf(abs(med.Bz{k}))
    xlabel('x');ylabel('y');legend(['dz = ' num2str(k-1) 'mm']);
    zlim([0 1.3]);
end